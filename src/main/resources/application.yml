spring:
  application:
    name: "apigateway"
  config:
    import: "configserver:http://localhost:8071"
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false
          lower-case-service-id: true
    config:
      name: apigateway, common